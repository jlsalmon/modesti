image: digitallyseamless/nodejs-bower-grunt

before_script:
  - npm install --silent
  - bower install

ping:
  script:
    - ping -c 5 artifactory

grunt:
  script:
    - grunt publish:test --verbose
