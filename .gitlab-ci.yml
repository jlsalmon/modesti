image: digitallyseamless/nodejs-bower-grunt

before_script:
  - npm install --silent
  - bower install

grunt:
  script:
    - grunt publish:test --verbose --artifactoryUser=${ARTIFACTORY_USERNAME} --artifactoryPassword=${ARTIFACTORY_PASSWORD}
